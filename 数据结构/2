#include<stdio.h>
#include<stdlib.h>
#include<string.h>

#include"linklist.h"

linklist linklist_create(void)
{
	linklist H = (linklist *)malloc(sizeof(listnode));
	if(H == NULL)
	{
		printf("malloc failed!\n")
		return -1;
	}
	//赋初值
	H->data = 0;
	

}
